# body-1 image slot

Drop the **body-1** image (the "How it works" screenshot) into this folder (any filename).

- Single image: drop one file here (`png`, `jpg`, `jpeg`, `gif`, `svg`, `webp`).
- Light/dark pair (opt-in): drop two files matching `*-light.*` + `*-dark.*` or `light.*` + `dark.*`.

Then re-run the readme-article-expander skill to fill the `body-1` slot from this folder.
